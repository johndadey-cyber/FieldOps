{
  "name": "fieldops",
  "version": "1.0.0",
  "description": "FieldOps is a scheduling and job assignment application for service teams.",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test:ui": "playwright test",
    "build:tui-calendar": "esbuild node_modules/@toast-ui/calendar/dist/toastui-calendar.js --bundle --minify --format=esm --outfile=public/js/tui-calendar.min.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@toast-ui/calendar": "^2.1.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.2",
    "esbuild": "^0.19.12"
  }
}
